export function getFreeLinks(id: string) {
   const intermediateMirrorOne = `https://ww1.frkp.cc/film/${id}/`;
   const intermediateMirrorTwo = `https://flcksbr.top/film/${id}/`;

   return { intermediateMirrorOne, intermediateMirrorTwo };
}
